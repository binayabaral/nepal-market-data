name: Scrape NEPSE

on:
  schedule:
    # 4:00 PM Nepal Time (UTC+5:45) = 10:15 UTC, Monday-Friday. Not earlier: NEPSE closes at 15:00
    # but sharesansar posts the finished session 15:35-15:50, so a close-time run scraped yesterday.
    - cron: '15 10 * * 1-5'
  workflow_dispatch:

concurrency:
  # All three workflows push to the same branch, so a plain push fails if another one landed a
  # commit since checkout. One shared group means they queue instead of racing; the rebase retry
  # in the push step covers whatever still slips through, such as a human commit.
  group: commit-data
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6
        with:
          version: 11.14.0
          run_install: false

      - uses: actions/setup-node@v7
        with:
          node-version: '22'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Scrape NEPSE stock prices
        run: pnpm tsx scripts/daily/scrape-nepse.ts

      - name: Commit and push data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/nepse data/reference
          if git diff --cached --quiet; then
            echo "No changes to commit."
            exit 0
          fi
          # Subject names the dates actually staged: rows are source-dated, not run-dated.
          git commit -m "$(bash scripts/ci/data-commit-message.sh 'chore: update NEPSE data')"
          for attempt in 1 2 3; do
            if git push; then exit 0; fi
            echo "Push rejected (attempt $attempt), rebasing onto the latest ${GITHUB_REF_NAME:-main} and retrying."
            git pull --rebase --autostash origin "${GITHUB_REF_NAME:-main}" || exit 1
          done
          echo "Could not push after 3 attempts."
          exit 1
